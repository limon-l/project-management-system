version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: boardflow-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: boardflow

volumes:
  mongodb_data:
